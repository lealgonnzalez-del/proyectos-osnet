services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mi-proyecto-practica
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
